<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.payzout.business">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".auth.RegistrationActivity"></activity>
        <activity android:name=".transaction.TransactionActivity" />
        <activity android:name=".portfolio.PortfolioActivity" />
        <activity android:name=".profile.QRActivity" />
        <activity android:name=".profile.KycActivity" />
        <activity android:name=".common.MainActivity" />
        <activity android:name=".ProfitCalculatorActivity" />
        <activity android:name=".auth.CodeActivity" />
        <activity android:name=".auth.PhoneActivity" />
        <activity android:name=".common.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>